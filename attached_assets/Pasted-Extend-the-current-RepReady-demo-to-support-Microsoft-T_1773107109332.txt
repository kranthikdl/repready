Extend the current RepReady demo to support Microsoft Teams as an integration surface for live coaching input.

Important framing:
RepReady is still a standalone real-time SDR coaching product.
Microsoft Teams is an input/integration channel, not the entire product.
Do not redesign the product, remove simulation mode, or replace the current standalone live mode.
Add Teams as a new transcript/audio ingestion path in a modular way.

Primary goal:
Add a new mode called "Teams Mode" so the app can eventually support live coaching during Microsoft Teams meetings.

Critical constraint:
Do not fake full production-grade Teams media integration if that is not realistically possible in the current environment.
Implement the integration in progressive layers:
1. detect and prepare for Teams embedding / Teams session context
2. support ingestion of Teams meeting transcript or meeting metadata if available
3. preserve fallback behavior if direct media/transcript access is not available
4. keep the current standalone browser demo working

What to build:

1. Add a new session mode
Current modes:
- simulation
- live

Add:
- teams

Update all relevant frontend and backend types to support:
mode: "simulation" | "live" | "teams"

2. Add a Teams integration layer
Create a modular TeamsIntegration service / adapter.
This layer should be separate from the coaching engine.

Responsibilities:
- initialize Microsoft Teams SDK if running inside Teams
- detect whether the app is opened inside a Teams context
- capture available Teams context metadata if accessible, such as:
  - meeting title
  - meeting id
  - user display name
  - tenant id
  - conversation / chat context if provided
- expose a clean normalized session context object to the app

3. Keep transcript source abstraction clean
The app should already support transcript sources.
Extend the transcript source abstraction to include a TeamsTranscriptSource.

Needed transcript sources:
- SimulationTranscriptSource
- LiveTranscriptSource
- TeamsTranscriptSource

All transcript sources must emit the same normalized transcript event shape:
{
  "text": "string",
  "speaker": "rep" | "buyer" | "system",
  "timestamp": number,
  "isFinal": true
}

4. Implement Teams mode in realistic phases
Do not overclaim.
Implement in this order:

Phase A: Teams app shell + context detection
- Add Microsoft Teams JS SDK
- Detect whether app is opened inside Teams
- Show Teams status in UI:
  - not in Teams
  - Teams SDK loaded
  - Teams context detected
  - transcript unavailable
- If app is not opened inside Teams, show clear fallback guidance

Phase B: Meeting metadata ingestion
- If Teams context is available, capture and display relevant meeting/session metadata
- Attach metadata to saved session artifacts

Phase C: Transcript ingestion path
Implement the best realistic Teams transcript path available in code architecture.
If true direct Teams transcript ingestion is not available in the current setup, do NOT invent it.
Instead:
- stub the Teams transcript provider cleanly
- document exactly what additional Microsoft Graph / Teams APIs / permissions would be required
- allow manual or mocked Teams transcript event injection so Teams mode can still be demonstrated architecturally

5. Preserve existing product logic
Do not rewrite:
- coaching engine
- scoring logic
- summary generation
- session artifact storage
- simulation mode
- standalone live mode

Only extend the transcript/input surface and session context layer.

6. UI changes
In Session Setup:
- add "Teams Mode" as a mode option
- show a short explanation:
  "Use Microsoft Teams as the meeting context/input surface when available."

In Live Session / Session View:
- add Teams status card showing:
  - Teams SDK loaded or not
  - running inside Teams or not
  - meeting context available or not
  - transcript availability
- if in Teams mode but transcript is unavailable, show:
  - fallback option to use simulation
  - fallback option to use standalone live browser mic mode
- display Teams meeting metadata when available

7. Progressive fallback behavior
Teams mode must never hard-break the app.
If Teams SDK is unavailable or transcript cannot be obtained:
- do not crash
- show clear limitation message
- allow easy fallback to:
  - simulation mode
  - standalone live mode

8. Add a clear architecture boundary
Create or update these modules/services:
- TeamsIntegrationService
- TeamsTranscriptSource
- TeamsContextProvider

The coaching engine must remain unaware of whether transcript came from:
- simulation
- browser live mic
- Teams

9. Microsoft dependencies
Use official Microsoft packages where appropriate, such as Teams JS SDK.
Do not invent fake SDK methods.
If Graph permissions or meeting transcript APIs are needed, document them clearly in comments and README.
If some pieces cannot be fully completed in local Replit runtime, implement clean placeholders and explicit TODOs rather than pretending they work.

10. README updates
Add a "Microsoft Teams Integration" section that explains:
- what works now
- what is scaffolded
- what requires Microsoft 365 / Teams app registration
- what permissions or APIs would be needed for true meeting transcript ingestion
- how to test the app in standalone mode vs Teams mode
- known limitations of Replit/local testing for Teams integration

11. Code quality requirements
- TypeScript throughout
- Clean modular code
- No pseudo-code
- No fake success states
- Add comments where Teams SDK or Graph constraints matter
- Preserve runnable app behavior
- Ensure existing demo still works without Teams

12. Nice-to-have if feasible
- A mocked Teams meeting context for local testing
- A "Demo Teams Context" toggle for architecture demonstration when not actually inside Teams
- A developer utility for injecting mocked Teams transcript events into the pipeline

Important guardrails:
- Do not claim full production Teams meeting audio capture unless actually implemented
- Do not remove simulation mode
- Do not replace live browser mic mode
- Do not rewrite the product as a Teams-only app
- Do not add unrelated integrations
- Do not overbuild enterprise auth unless needed for Teams scaffolding
- Keep this scoped to adding Teams as an integration surface

Deliverable:
Return a full updated runnable codebase.
After implementation, summarize:
1. what Teams capabilities are actually working now
2. what is scaffolded but requires Microsoft permissions/app registration
3. which files changed
4. how Teams mode falls back when transcript access is unavailable